import styled from "styled-components";

export const Container = styled.header`
  button {
    background: none;
    color: var(--color-secondary);

    font-size: 1.125rem;

    transition: all 0.3s;

    &:hover {
      color: #fff;
    }
  }
`;
