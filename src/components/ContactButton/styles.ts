import styled from "styled-components";

export const Container = styled.button`
  background: none;
  color: var(--color-secondary);

  font-size: 1.125rem;
  font-weight: 500;

  transition: all 0.3s;

  &:hover {
    color: #fff;
  }
`;
